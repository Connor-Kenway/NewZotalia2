import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { useLocalSearchParams } from 'expo-router';

export default function TopFieldOfWork() {
    //need a return to not get unmatched route error
    return;
}